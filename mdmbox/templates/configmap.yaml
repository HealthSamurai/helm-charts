apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mdmbox.fullname" . }}
  labels:
    {{- include "mdmbox.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
