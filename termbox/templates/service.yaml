apiVersion: v1
kind: Service
metadata:
  name: {{ include "termbox.fullname" . }}
  labels:
    {{- include "termbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "termbox.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
