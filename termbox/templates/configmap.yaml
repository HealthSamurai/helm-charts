apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "termbox.fullname" . }}-config
  labels:
    {{- include "termbox.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
