apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "smartbox.fullname" . }}-config
  labels:
    {{- include "smartbox.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
